-- 库存 + 价格 + 图片（看板会模糊匹配列名，含 ImageUrl）
-- 导出后直接覆盖 Output-NZ/stock.csv 和 stock.xlsx（不再建时间戳子文件夹）
-- 建议列名：Sku/ProductCode, ProductName, ProductFamily, StockQty, UnitPrice, IsDiscontinued, ImageUrl
-- SELECT
--   p.Code AS ProductCode,
--   p.Name AS ProductName,
--   p.Family,
--   s.Qty AS StockQty,
--   p.Price,
--   p.Discontinued,
--   p.ImageUrl
-- FROM ...

SELECT GETDATE() AS run_time;
